version: "3.9"
services:
  web-app:
    build: ./web-app
    ports:
      - "80:80"
  image-server:
    build: ./image-server
    ports:
      - "8080:8080"
  gdal-server:
    build: ./gdal-server
    ports:
      - "5000:5000"